## Restatify AI Multichat 2.1.2

- Adds a private WordPress bridge for the public Support API.
- Supports production deployments where WordPress and Support API run on separate servers with different public IPs and FQDNs.
- Preserves WordPress-backed support replies, API key management, booking overlay triggers, AI mode changes and visitor token validation across the new server-to-server boundary.